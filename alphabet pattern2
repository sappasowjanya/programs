#include <stdio.h>

int main(void) {
	// your code goes here
int n,i,j,k;
	scanf("%d",&n);
	for(i=0;i<n;i++)
	{
	    for(j=0;j<n-i-1;j++)
	    {
	        printf(" ");
	    }
	    for(k=j;k<n;k++)
	    {
	        printf("%c",i+65);
	    }
	    printf("\n");
	}
	return 0;
}
