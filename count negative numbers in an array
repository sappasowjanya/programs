#include<stdio.h>
int main(int argc, char *a[])
{
	//Write code here
	int n,c=0;
	int i;
	scanf("%d",&n);
	int b[n];
	for(i=0;i<n;i++)
	{
	    scanf("%d",&b[i]);
	   if(b[i]<0)
	   {
	       c++;
	   }
	}
	printf("%d",c);
	return 0;
}
